micronaut:
  application:
    name: msg-platform-persistence-jdbc-test

db:
  dialect: H2

datasources:
  default:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: ''
    dialect: H2
    maximum-pool-size: 5
    minimum-idle: 1
